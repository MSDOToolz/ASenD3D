class InputConverter:

    def __init__(self):
        return

    def convertInput(self,inputFile,inputFormat,outputFile):
        if(inputFormat == 'abaqus'):
            self.convertAbaqus(inputFile,outputFile)

    def convertAbaqus(self,inputFile,outputFile):
        return

